plugins {
    id 'application'
    id 'java'
}

group 'ca.voidustries.guild'
version 'alpha'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
    implementation 'org.javacord:javacord:3.1.2'
    compile 'joda-time:joda-time:2.10.9'
}

test {
    useJUnitPlatform()
}

mainClassName = 'ca.voidustries.Guild.Main'
